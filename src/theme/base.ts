// Canvas format and safe area — the two constants every other theme module
// and the compiler derive from.

export const format = {
  width: 1080,
  height: 1920,
  fps: 30,
};

export const safeArea = {
  top: 90,
  right: 70,
  bottom: 150,
  left: 70,
};
